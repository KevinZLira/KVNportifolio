[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Single-page app: let React Router handle every path client-side.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
